// Astro auto-generates ../.astro/types.d.ts on build. ESLint disallows the
// triple-slash reference style, so we import the types directly instead.
// eslint-disable-next-line @typescript-eslint/triple-slash-reference
/// <reference path="../.astro/types.d.ts" />
